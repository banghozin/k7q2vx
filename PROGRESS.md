# 진행 기록

> 한 일과, 봐 주셔야 할 것. 최근 것이 위입니다.

---

## 지금 상태 한눈에 (2026-08-27 마지막 갱신)

| | |
|---|---|
| 라이브 | https://stocksinfo.vercel.app |
| 깃허브 | https://github.com/banghozin/k7q2vx (공개) |
| 밀린 커밋 | 없음 |

### 만들어진 기능

| 화면 | 무엇 |
|---|---|
| `/` | 테마 11개, 한 줄 브리핑, 지난 N일 요약, 뜨거운·식은 층, 손바뀜, 미국 시장 헤드라인 |
| `/theme/[slug]` | 층 지도, 층 온도, 층 순환 시간축, **동조율**, 대장주 근거, 테마 뉴스 |
| `/analyze` | **차트 분석** — 종목·봉단위를 골라 실제 차트에 선을 긋고, 그린 것이 기기에 남음. 주요 고저점·피보나치 자동 찾기. 매매노트 회고 모드 |
| `/practice` | **차트 훈련** — 과거로 돌아가 가린 채 분석하고 실제를 겹쳐 봄 + 훈련 기록 |
| `/news` | **뉴스 보관함** — 지난 기사를 날짜·시각별로. 한국어(SBH) + 미국 금융 매체 6곳 |
| `/notes` | 매매노트 — R 배수, 원칙 준수, 이유별 통계, 진입일 기사, **진입 때 그린 차트 얼려 붙이기** |
| `/watchlist` | 별표 종목 + 층 쏠림 경고 |
| `/about` | 읽는 법 · 계산 방식 · 지켜야 할 선 |

### 자동으로 도는 것

| 언제 | 무엇 |
|---|---|
| 평일 미국장 마감 후 (UTC 22:17) | 티커 생존 검사 → 시세 수집 → 지표 계산 → 뉴스 → 커밋 |
| **6시간마다** (UTC 2·8·14·20시 11분) | 뉴스만 수집 → 바뀌었으면 커밋 |

**둘 다 예약 실행이 실제로 도는 것을 확인했습니다.** 시세는 2026-08-25 밤,
뉴스 6시간 수집은 2026-08-26 14:58 과 23:26 에 `github-actions[bot]` 이 스스로
커밋했습니다.

### 기기에 저장되는 것 (localStorage, 서버로 안 감)

매매노트 · 워치리스트 · 훈련 기록 · **분석 화면에 그린 것**

---

## 2026-08-27 · 점검에서 나온 것들 (고침 여덟)

붙인 것을 다시 훑어 실제로 틀린 것을 찾았습니다. 대부분 **화면에 그대로 보이던
것**입니다.

### ① 월봉이 종목에 따라 분기봉·주봉이었다 ★ 제일 컸다

`range=max` 는 **요청한 봉 단위를 통째로 무시합니다.** 전체 기간 길이를 보고
야후가 제 마음대로 정합니다. 전부 `interval=1mo` 로 요청하고 재어 본 것:

| 종목 | 상장 | max 로 실제 온 것 |
|---|---|---|
| ETN·MU | 1984 | **3개월봉(분기봉)** |
| NVDA | 1999 | 1개월봉 |
| IONQ | 2021 | **1주봉** |
| GEV | 2024 | **1주봉** |

기간을 명시하면 어떤 값이든 요청한 단위가 그대로 옵니다. `40y` 로 바꿨습니다
(ETN 168봉 → 480봉). 응답에 실제로 온 단위(`dataGranularity`)를 같이 넘겨
나중에 되짚을 수 있게 했습니다.

> 메모리에 "max 는 월봉으로 뭉개진다"고 적혀 있었는데 **NVDA 하나만 보고 내린
> 결론이라 틀렸습니다.** 고쳤습니다.

### ② 주봉·월봉에 가짜 봉이 하나 더 있었다

야후가 진행 중인 기간 봉을 덧붙입니다. 앞 봉이 이미 오늘까지 반영하고 있어
값이 똑같습니다 → **등락률이 0.00%** 로 나왔습니다. 간격 중앙값으로 재는
방법은 월봉에서 안 통합니다(08-01→08-26 은 25일이라 평소 30일의 절반 초과).
봉 단위별 최소 간격으로 판정합니다.

### ③ `clean-news` 가 아카이브를 84% 지울 뻔했다

영문 피드를 붙인 뒤 돌려 보니 **414건 중 348건**을 버리려 했습니다. 제대로
걸린 영문 기사(CSCO·CRWV·INTC)와 시장 기사까지요. 한국어 판정만 제목에 돌려
아무것도 안 걸리면 버리는 구조였습니다. 하필 CLAUDE.md 와 메모리 양쪽이
"잘못 걸린 기사를 빼려면 `npm run clean-news`" 라고 안내하고 있었습니다.

버릴 수 있는 것을 좁혔습니다 → **8건**. 남은 후보에도 본문에서 걸린 정상
기사가 섞이므로 "버림" 이 아니라 "후보" 로 적고 경고를 답니다.

### ④ 낱말 순서에 따라 종목을 놓쳤다

나온 자리를 **처음 것만** 봤습니다.

```
"lucid dreaming … Lucid shares rose"  → 못 잡음
"Lucid shares rose … lucid dreaming"  → 잡음
"intel sources say Intel will delay the fab" → 통째로 놓침
```

전부 훑어 하나라도 이름 꼴이면 걸린 것으로 봅니다. 함께 나온 둘:
**소문자로 시작하는 상표**(nVent — iRobot·eBay 꼴이 전부 여기 걸립니다),
**앞의 The**(야후는 "The Boeing Company" 라 주는데 기사는 "Boeing" 이라 씁니다.
Boeing 과 Bank of New York Mellon 을 둘 다 놓치고 있었습니다).

회귀 테스트 24 → 30가지.

### ⑤ 피드가 주는 값을 그대로 믿었다

`javascript:` `data:` 링크와 상대경로가 그대로 `href` 로 들어갔습니다. 상대경로는
**우리 도메인**으로 풀립니다. http(s) 절대주소만 받고 제목은 300자에서 끊습니다.
기존 아카이브 506건에는 없었습니다.

한 건도 못 받은 피드를 실행 때마다 찍습니다 — 피드는 예외 없이 조용히 죽습니다.

### ⑥ 자동 고저점이 누를 때마다 겹쳐 쌓였다

민감도를 촘촘히↔크게로 바꿔 견주는 게 이 기능을 쓰는 보통의 방식인데, 앞서
그린 16줄이 그대로 남았습니다. 이제 자기가 그린 것만 지우고 새로 긋습니다
(16→16→16). 자동 피보나치도 같은 자리면 갈아 끼웁니다.

### ⑦ `typeof x === "number"` 는 NaN 을 통과시킨다

그런 봉 하나가 섞이면 **고저점 검출이 통째로 멈춥니다** — NaN 과의 비교는
전부 거짓이라 방향이 영영 안 정해집니다. API 와 `detectSwings` 양쪽에서 막고,
0·음수도 걸러냅니다.

### ⑧ 분석 그림 저장에 상한이 없었다

localStorage 는 5MB 에서 막히고 넘치면 저장이 **예외를 던집니다.** 종목 하나에
봉 단위가 여덟이라 생각보다 빨리 늡니다. 훈련 기록이 200벌에서 끊는 것과 같은
방식으로 120벌에서 끊습니다.

### 남긴 도구 — `npm run test:data`

점검하며 일회용으로 쓴 것이 값을 해서 상시 도구로 남기고 **갱신 워크플로
두 개의 커밋 앞단**에 물렸습니다. 일부러 망가뜨려 잡히는 것까지 확인했습니다.

### 검사했는데 이상 없던 것

- 콘솔 오류·경고 — 8개 화면 전부 0건
- 모바일 가로 넘침 — 7개 화면 전부 0px
- 생성 데이터 무결성 — 190종목·188판정·269기사 문제 없음
- 아무 데이터 없는 상태(새로 클론)에서 빌드·모든 화면 200
- 훈련 화면 월봉 조건 — 표본의 63%가 통과, 8번 시도해 전부 실패할 확률 0.03%
- 뉴스 아카이브가 브라우저 번들로 새지 않음

### 지켜볼 것 (안 고침)

- **클라이언트 번들 390KB** — `stocks`·`sync`·`moves` JSON 이 한 청크에 실립니다.
  brotli 로 80KB 라 급하지 않고, 고치려면 잘 도는 코드를 뜯어야 합니다
- **`/news` 한 페이지에 전부 그림** — 지금 269건, 상한 3000건. 종목 기사 90일 +
  시장 기사 10일 창이라 1400건쯤에서 멎을 것으로 봅니다
- RSS 파싱기가 한국어용·영문용 둘로 갈려 있습니다. 필요가 달라 일부러 뒀습니다

---

## 2026-08-27 · 기존 두 사이트에서 건질 것을 골라 넷을 붙였다

stocksnet 과 chart 폴더를 코드까지 열어 보고 **가져올 것과 버릴 것**을 갈랐습니다.
가져온 넷이 이날 작업 전부입니다.

### ① 미국 금융 매체 뉴스 (가장 큰 것)

뉴스가 부실했던 건 판정 로직 탓이 아니라 **공급원이 한국 매체 하나뿐**이라서였습니다.
stocksnet 이 이미 영문 RSS 를 직접 읽고 있었습니다. 그대로 가져왔습니다.

CNBC(마켓·종합)·MarketWatch·Yahoo Finance·Nasdaq·Seeking Alpha 여섯 곳.
**CNBC 마켓 피드는 실측 375시간(약 15일)치**를 줍니다 — SBH 의 7.6시간과 비교가
안 됩니다. 수집 한 번에 3건 잡히던 것이 **145건**이 됐습니다.

영문은 판정 규칙을 따로 뒀습니다(`src/lib/en-match.ts`). 한국어 규칙을 그대로
쓰면 안 됩니다 — 영어는 회사 이름이 일상어와 훨씬 자주 겹칩니다.

| 걸릴 뻔한 것 | 이유 |
|---|---|
| 연준 의장 파월 기사 → POWL | Powell Industries |
| 폭동(Riot police) 기사 → RIOT | Riot Platforms |
| 양자컴퓨팅 기사 → QUBT | 회사 이름이 Quantum Computing Inc |
| "The company applied for…" → AMAT | Applied Materials |
| "Net income rose…" → NET | Cloudflare 의 티커 |
| "meta-analysis" → META | 하이픈 뒤 |

지킨 규칙 둘: **대소문자를 구별**하되 야후 표기(`NVIDIA`)와 기사 표기(`Nvidia`)가
달라서 철자는 무시하고 **낱말마다 대문자로 시작하는지**를 봅니다. 티커를 맨몸으로
잡는 건 안전한 것만이고 나머지는 `(NVDA)` `$NVDA` `NASDAQ: NVDA` 같은 **관례
표기**일 때만 잡습니다.

회귀 테스트 24가지: `npm run test:en`. 회사명은 손으로 안 적고 야후에서 받습니다:
`npm run names` → `src/data/generated/names.json`.

홈 헤드라인은 최신순이 아니라 **우리 종목이 나온 기사를 먼저** 올립니다. 최신순으로
뒀더니 무명 종목 EPS 단신이 맨 위에 왔습니다.

### ② 분석 그림을 매매에 얼려 붙이기

새 매매를 기록할 때 그 종목으로 그려 둔 분석을 함께 붙일 수 있습니다.
**참조가 아니라 복사입니다.** 분석 화면의 그림은 종목·봉 단위마다 한 벌뿐이라
그대로 가리키면 나중에 선을 고칠 때 **지난 매매의 근거가 소급해서 바뀝니다.**

매매 카드의 "그때 그린 차트 보기"는 `/analyze?trade=<id>` 로 열립니다(회고 모드).
얼려 둔 그림 위에 진입·손절·청산을 수평선으로 얹고, 차트는 최신 봉까지 다 그리므로
**그때 그은 선 위로 이후 값이 어디로 갔는지가 겹쳐 보입니다.**

> **회고 모드에서는 자동 저장을 끕니다.** 안 끄면 지난 매매를 열어 보는 것만으로
> 지금 분석이 덮어써집니다. 헤드리스로 확인했습니다 — 회고를 연 뒤에도 시트의
> `updatedAt` 이 한 글자도 안 바뀌었습니다.

### ③ 주요 고저점·피보나치 자동 찾기

chart 폴더의 지그재그 검출을 가져왔습니다. 그 폴더에서 이것만 가져온 이유는
**판단이 아니라 눈금**이기 때문입니다. 매수·매도 점수 합산은 버렸습니다(아래 참고).

되돌림 임계값을 고정하지 않고 **그 종목이 실제로 움직이는 폭에서 잡습니다.**

| | NVDA | ETN | MU | EQIX | IONQ |
|---|---|---|---|---|---|
| 하루 변동 중앙값 | 1.64% | 1.09% | 2.61% | 0.82% | 3.77% |
| "보통" 기준 스윙 수 | 27 | 46 | 30 | 37 | 22 |

고정 6% 를 쓰면 NVDA 는 93개인데 EQIX 는 거의 안 잡힙니다. 봉 단위별 표도 만들어
봤지만 같은 일봉이라도 종목 간 변동폭이 몇 배씩 차이 나 소용이 없었습니다.

척도는 **봉과 봉 사이 종가 변동의 중앙값**입니다. 봉 하나의 고저폭도 재 봤는데
잔떨림까지 담겨 값이 부풀고, 월봉에서는 22%까지 올라가 어떤 배수를 곱해도 상한에
붙었습니다(종가 변동 기준은 9.99%).

### ④ 층 전체였나, 이 종목만이었나

밤에 종목이 빠졌을 때 가장 먼저 궁금한 것입니다. `moves.json` 을 새로 만들고
차트 모달에 붙였습니다.

그 종목이 크게 움직인 날 같은 층의 **나머지** 종목 중앙값과 견줍니다. 자기를 빼는
이유는 구성원이 적은 층에서 자기가 자기를 설명하게 되기 때문이고, 중앙값을 쓰는
이유는 한 종목이 실적으로 튀어도 층 전체가 왜곡되지 않게 하기 위해서입니다.
**비교 상대가 하나뿐이면 판정하지 않습니다** — 그건 중앙값이 아니라 그냥 그 한
종목입니다.

기준일(8/25) 판정 75건. 예: `SMCI +9.35% · AI 7층 나머지 5종목 중앙값 +2.00%
→ 이 종목만`. 바로 아래 같은 날 기사("대만, AI 서버 밀수 혐의 기소…슈퍼마이크로
직원 포함")가 붙어 서로를 설명합니다.

> 제목 옆에 **기준일을 반드시 붙였습니다.** 모달 머리의 시세는 지금 값이라,
> 날짜가 없으면 "왜 지금 빠지는데 올랐다고 하지" 로 읽힙니다. OKLO 에서 실제로
> 그렇게 보였습니다.

---

## 2026-08-27 · 조용한 매집은 재 보고 버렸다

넣을 뻔했다가 **우리 데이터로 재 보고 뺐습니다.** 기록으로 남깁니다.

가설: 거래량이 20일 평균의 3배인데 종가는 ±2% 안이면 누군가 조용히 사 모은 것이다.

188종목 2년 일봉으로 셌습니다.

```
기준선(전체 거래일)   n=85,846   20일 뒤 중앙값 +1.58%   상승 55.2%
조용한 대량거래       n=   216   20일 뒤 중앙값 +1.74%   상승 55.6%
```

**구별이 안 됩니다.** 기준을 20가지 조합으로 바꿔 봐도 기준선 주변을 방향 없이
오갑니다. "거래량 4배 + 등락 1% 미만" 은 −1.52% 인데 바로 옆칸 "4배 + 2% 미만" 은
+3.80% 입니다 — 조용할수록 강해져야 논리가 맞는데 정반대로 뒤집힙니다. 표본이
적어서 나는 요동입니다.

발생률도 전체 거래일의 0.25%, 종목당 2년에 1.1회, 7월 이후 두 달간 190종목에서
5건입니다. 층별로 나누면 대부분의 층이 몇 달째 0건이라 화면이 비어 있습니다.

정확한 문장은 "이 통설이 틀렸다" 가 아니라 **"우리 데이터에서는 구별되지 않는다"**
입니다. 한계 셋: 손으로 고른 중·대형주 190종목이고, 일봉 종가만 봤고, 2년치입니다.

**나중에 할 만한 것** — 이 방식을 화면으로 만들 수 있습니다. 골든크로스·52주 신고가
돌파 같은 통설을 우리 데이터로 재서 보여 주는 것. 추천이 아니라 **검산**이라 선 안에
들어오고, 국내에 대체제가 없습니다.

---

## 2026-08-27 · 가져오지 않기로 한 것

기존 두 사이트를 열어 보고 **안 가져오기로 한 것**과 그 이유입니다. 다시 제안하지
않도록 남깁니다.

| 후보 | 왜 안 가져오나 |
|---|---|
| 매수/매도 점수 합산 | RSI 과매도(평균회귀)와 EMA 정배열(추세추종)은 **반대되는 이론**인데 한 통에 넣고 셌다. "매수 3 : 매도 2" 의 3 에 의미가 없다 |
| 신호 승률 · 백테스트 | CLAUDE.md 6번 선에 정면 충돌 |
| 섹터 히트맵 | **업종 기준**이다. "업종 말고 층으로 보자" 가 전부인 사이트에서 주장이 스스로 무너진다 |
| 포트폴리오 중복 분석 | 워치리스트의 층 쏠림 경고와 겹친다. 분기 1회 갱신되는 정적 ETF 매핑(자기 문구에 ±5% 오차 명시) |
| DART 임원 매수 | 국내 종목 |
| RSI·MACD·볼린저 직접 구현 | klinecharts 에 이미 있다. 버튼만 늘리면 된다 |
| 삼각수렴 자동 감지 | 임의 파라미터가 많아 "패턴이 있다" 는 주장이 된다 |
| 가격 알림·푸시 | 서버·DB 필요 → 유지비 0원 깨짐 |
| 뉴스 제목 자동 번역 | LLM 호출은 매달 돈이 나간다 |

**나중에 할 만한 것** — 13F 를 **층 단위로 합산**하기(SEC 무료, 분기 1회, 국내외에
같은 게 없음. 단 펀드 30~50곳은 모아야 표본이 됨), 실적 발표를 층 위에 얹기(한국시간
표시 + 그 층 동조율), 미국장 시계·휴장 달력.

---

## 2026-08-26 · 밀린 커밋을 올리고 데이터를 다시 계산

한도가 풀려 7개 커밋을 한 번에 푸시했습니다. 그 과정에서 **봇이 먼저 올려둔
데이터 커밋과 충돌**했는데, 충돌은 생성된 JSON 에만 있고 코드에는 없었습니다.

숫자를 손으로 합치지 않고 **다시 계산하는 쪽**을 골랐습니다. 봇은 층 순환
코드가 없는 상태에서 돌았기 때문에 그 결과를 그대로 두면 `rotation.json` 만
빠진 어중간한 데이터가 됩니다. 코드를 먼저 얹고 `npm run update` 로 전부
다시 만들었습니다.

- 티커 190개 전부 생존 확인
- 재계산 결과 손바뀜 5건(AI 반도체 SNDK→ASML, 전력 PWR→NRG, 로봇 CGNX→RRX,
  보안 CRWD→RPD, 자율주행 ON→APTV). 대부분 "접전" 표시가 함께 붙습니다
- 뉴스 5건 새로 보관(누적 6건)

**충돌이 또 나면 같은 방법으로 처리하세요**: 코드를 먼저 올리고 데이터는
다시 만든다. 생성 파일을 손으로 병합하지 않습니다.

---

## 2026-08-26 (심야) · 그리기 도구 고치기, 분석 페이지, 뉴스 보관함

### 보조지표가 안 빠지던 것 — 반환값을 잘못 알았습니다

한 번 켜면 다시 눌러도 안 꺼지고, 세 번째에 **같은 지표가 하나 더** 붙었습니다.

`createIndicator` 는 **판(pane) 번호가 아니라 지표 id** 를 돌려줍니다. 그걸
판 번호로 알고 저장해 두었으니 지울 때 못 찾았고, 저장해 둔 것도 비어 있어서
다시 누르면 새로 만들었습니다.

이제 따로 세어 두지 않고 **차트에 직접 물어봅니다**(`getIndicators({name})`).
세는 일 자체가 없어져 어긋날 수가 없고, 이미 중복으로 붙은 것도 정리됩니다.
검증: 캔버스 6 → 10 → **6** → 10.

### 도구가 한 번 쓰면 풀리던 것

"자유곡선을 골랐는데 화면만 움직이고, 한 번 더 누르면 그려지고, 다 그리면 또
풀린다"는 지적. 합성 이벤트로는 재현이 안 됐지만 **원인과 무관하게 옳은
구조**가 있어 그쪽으로 바꿨습니다 — **도구를 고르면 계속 물려 있습니다.**
하나 그리면 같은 도구가 곧바로 다시 물립니다. 같은 단추를 다시 누르거나
`Esc` 로 풉니다. 검증: 수평선을 한 번만 켜고 세 번 클릭 → 세 개.

### 그린 것 지우기

선을 클릭해 고른 뒤 `Delete`(또는 `⌫`). 모바일용으로 왼쪽 패널에
**"선택한 것 지우기"** 단추도 뒀습니다. 검증: 수평선 3 → 2.

주의: **도구가 물려 있으면 클릭이 "고르기" 가 아니라 "새로 그리기" 입니다.**
먼저 `Esc` 로 풀어야 고를 수 있습니다. 트레이딩뷰도 같습니다.

### 피보나치 색을 눈금마다 다르게

한 색의 진하기만 달리했더니 "38.2 인지 61.8 인지" 를 색으로 못 알아봤습니다.
0% 흰빛 · 23.6% 자주 · 38.2% 주황 · 50% 풀색 · **61.8% 하늘**(제일 많이 보는
자리) · 78.6% 연보라 · 100% 돌빛. 구간 배경도 그 색을 옅게 깝니다.

### 차트 분석 페이지 `/analyze`

훈련 화면이 *과거를 가리고 맞춰 보는 곳*이라면, 여기는 **지금 보는 종목에
실제로 선을 긋는 곳**입니다. 종목 검색 · 봉 단위 여덟 개 · 같은 그리기 도구 ·
보조지표.

**그린 것은 이 기기에 남습니다.** 같은 종목·같은 봉 단위로 다시 오면 그대로
복원됩니다. 좌표를 화면 위치(px)가 아니라 **시각·가격**으로 저장하므로
확대·축소를 해도 자리가 안 어긋납니다. 저장 단추를 안 눌러도 그릴 때마다
자동으로 남습니다 — 안 누르고 나간 사람이 통째로 잃지 않게.

검증: 선 세 개를 긋고 새로고침 → **"그린 것 3개"** 로 복원.

### 뉴스 보관함 `/news` 과 6시간 수집

"헤드라인 개수는 당일 기준인가" 라는 물음에 재어 봤더니 **아니었습니다.**
공개 피드는 최근 100건, 실측 **7.6시간치**입니다. 하루 한 번만 모으면 그날
기사의 3분의 1도 못 건집니다.

- **6시간마다** 모으는 워크플로를 따로 만들었습니다. 창이 겹쳐 사이가 안 빕니다
- 종목이 안 걸려도 **미국 시장 기사면 함께 보관**합니다. 전에는 종목 기사만
  남겨서 헤드라인이 실시간 피드에만 매여 있었습니다
- `/news` — 날짜·시각별로 넘겨 보고, 종목 태그를 누르면 분석 화면으로

**미국 기사는 미국장 시간대에 몰립니다.** 한국 낮에 확인하면 0건인 게
정상입니다(실측: 한국 정오 3건, 오후 5시 0건).

### 아이콘

지층 단면도 — 아래로 갈수록 넓고 어둡게, 맨 위 한 층만 황동색.
`icon.svg` 하나와, 애플용 PNG 는 빌드할 때 코드에서 그려 냅니다(애플은 SVG 를
안 받습니다). 그림 파일을 따로 두면 색을 바꿀 때 아이콘만 옛날 색으로 남습니다.

---

## 2026-08-26 (밤) · 차트를 트뷰처럼, 뉴스에서 국내 기사 걷어내기

### 과거가 안 나오던 것 — 데이터를 안 넣어 줬습니다

왼쪽으로 아무리 밀어도 과거가 없었습니다. **차트에 최근 180봉만 넣고 있었기
때문**입니다. 배율 문제가 아니라 데이터가 없던 것이었습니다.

지금은 **가린 시점까지의 전 기간**을 다 넣고 배율만 180봉에 맞춥니다.
트레이딩뷰처럼 왼쪽으로 끌면 상장 첫날까지 갑니다. `전체 보기`는 담긴 봉
전부를 한 화면에 넣습니다.

### 정답도 최신까지

120봉에서 끊던 것을 **가린 지점 뒤에 남은 전부**로 바꿨습니다. `끝까지`를
누르면 데이터의 마지막 봉, 곧 가장 최근까지 갑니다. 다만 너무 옛날을 가리면
정답이 몇 년치가 되어 한 화면에 안 들어오므로, 가리는 지점은 뒤로 120~600봉이
남는 자리에서 고릅니다.

### 봉 단위 여덟 개

`5분 · 15분 · 30분 · 1시간 · 4시간 · 일봉 · 주봉 · 월봉`.
단타는 분봉, 스윙은 시간·일봉, 장투는 주봉·월봉으로 훈련할 수 있습니다.

야후를 직접 재 보고 조합을 정했습니다. **`range=max` 를 주면 봉 단위와 무관하게
월봉으로 뭉개져 나옵니다** — 일봉으로 `max` 를 부르면 332봉(월봉)이 옵니다.
그래서 일봉·주봉은 `10y`, 월봉만 `max` 를 씁니다. 분봉은 보관 기간이 짧아
5·15·30분은 60일, 60분은 730일까지입니다. **4시간봉은 야후가 주지 않아
60분봉을 넷씩 묶어 화면에서 만듭니다.**

분·시간봉이면 축·십자선·머리글에 시각을 함께 보여줍니다.

### 뉴스에서 국내 기사를 걷어냈습니다

**홈** — `economy` 카테고리를 그냥 가져와서 현대차증권 조직 개편, 중기부
소상공인 상담회 같은 기사가 올라왔습니다. 미국주식 매매에는 쓸모가 없습니다.
**미국 시장 얘기이거나 우리가 다루는 회사 얘기인 것만** 남깁니다.
실제 피드로 재 보니 경제 기사 34건 중 3건이 남았습니다(미국 기술주 반등,
앤스로픽 매출 전망, 다우·월가 자금 흐름). 없는 날은 구획이 통째로 사라집니다.

막을 것을 나열하는 방식이 아니라 **통과 조건을 요구하는 방식**입니다.
국내 기업 이름을 다 적을 수는 없으니까요.

**테마 페이지** — 이쪽이 더 심각했습니다. 테마 키워드로 피드를 훑고 있어서

| 테마 | 걸려 있던 기사 |
|---|---|
| 사이버보안 | 나이지리아 군 증강, 멕시코군 병력 배치, 콜롬비아 군부 작전 |
| 조선·해운 | 시외버스 요금 인상, 조선 성리학자 묘소 문화유산 지정 |
| 우주·방산 | 호주 국방 특사 사직 |

"조선", "보안", "운임" 같은 낱말이 엉뚱한 데서 잡힌 것입니다.
**종목 이름이 실제로 나온 기사만 보관해 둔 아카이브**로 갈아끼웠습니다.
키워드 방식(`matchTheme`)은 아예 지웠고, 자리에 왜 지웠는지 적어 뒀습니다.

### 이름 별칭에서 나던 오탐

아카이브 쪽에도 오탐이 있었습니다. 회사명이 두 낱말 이상이면 첫 낱말도
별칭으로 쓰는데("엔비디아 코퍼레이션" → "엔비디아"), 그 첫 낱말이 일상어면
엉뚱한 기사가 걸립니다.

- `AI·디지털 일자리센터 개관` → **디지털** 리얼티(DLR)
- `사우디 원전 협정, 우라늄 농축` → **우라늄** 에너지(UEC)

고친 것:
- 일상어 첫 낱말을 제외 목록에 추가(디지털·우라늄·웨스트·플렉스·바이킹·
  오로라·헌팅턴 등 14개). **회사 전체 이름이 나오면 그때는 정상적으로 걸립니다**
- **영문 첫 낱말은 별칭에서 뺐습니다.** "AST 스페이스모바일"의 `AST` 를
  별칭으로 두면 FAST·LAST 안에서도 걸립니다. 이름 판정은 한글 경계만 보기
  때문입니다. 영문 약칭은 티커 판정이 앞뒤 영숫자까지 확인하며 이미 잡습니다
- 판정 코드를 `src/lib/news-match.ts` 로 꺼내 **테스트가 실제 코드를 쓰게**
  했습니다. 전에는 테스트가 경계 규칙만 봐서 별칭 만들기의 사고를 못 잡았습니다
- 실제로 터졌던 기사 5건을 회귀 항목으로 추가 (총 19개 통과)
- `npm run clean-news` — 별칭을 고친 뒤 이미 보관된 잘못된 기사를 걷어냅니다.
  기본은 보여주기만 하고 `--apply` 를 줘야 지웁니다

**주의: 아카이브 파일을 지우지 마세요.** 피드는 최근 100건(약 7시간치)만
주므로 지나간 기사는 되찾을 수 없습니다. 정리하다 실제로 한 번 날렸고
`git checkout` 으로 되살렸습니다.

---

## 2026-08-26 (저녁) · 그리기 도구 손보기와 고딕 전환

### 파동 도구의 점이 하나씩 모자랐습니다

**ABC 조정이 Z 자가 안 나왔습니다.** A·B·C 세 점만 찍게 만들어 두어서 선분이
둘뿐이었고, 그러면 V 자가 됩니다. 조정파는 *출발점* 에서 시작해 A·B·C 로
가는 **네 점**입니다. 12345 파동도 0 에서 출발해 여섯 점, abcde 도 여섯 점으로
고쳤습니다. 트레이딩뷰의 엘리어트 도구와 같은 방식입니다.

### 색과 굵기를 고를 수 있습니다

왼쪽 맨 위에 **펜**이 생겼습니다. 색 여섯, 굵기 셋.

- 고르고 그리면 그 색으로 그려집니다
- **이미 그은 선을 클릭해 고르면 그 선의 색·굵기가 바로 바뀝니다.**
  패널 제목이 "추세선 색·굵기" 처럼 바뀌어 지금 뭘 고쳤는지 보입니다

만들어 둔 도구들은 색을 코드에 박아 뒀었는데, 그러면 골라도 안 먹힙니다.
그릴 때 넘긴 값을 도형이 읽도록 전부 고쳤습니다.

### 자유곡선 · Ctrl+Z

- **자유곡선** — 누른 채로 끌면 그려집니다. 기본 도구(brush)는 각져서
  손으로 그은 느낌이 안 나, 부드럽게 잇고 고른 색을 따르는 것으로 새로 만들었습니다
- **Ctrl+Z**(맥은 ⌘Z)로 되돌립니다. 되돌리기 단추는 원래 있었지만 목록 아래라
  눈에 안 띄었습니다. 단추에 단축키를 적어 두었습니다.
  글자 칸에 있을 때는 가로채지 않습니다

### 정답을 열면 쭉 진행됩니다

열어볼 수 있는 봉을 **60개(석 달) → 120개(반년)** 로 늘렸습니다. 그리고
**열 때마다 뒤로 물러나** 열린 만큼 화면에 다 들어오게 했습니다. 이게 없으면
배율이 그대로라 새 봉이 오른쪽 밖으로 밀려 조금밖에 안 보였습니다.

### 일봉인데 시각이 붙던 것

축과 십자선에 `2025-06-24 22:30` 처럼 시각이 붙었습니다. 미국장 마감을 한국
시각으로 옮긴 숫자라 "이거 일봉 맞나" 싶게 만듭니다. 날짜만 남겼습니다.

### 실시간 미리보기는 원래 됩니다

"점을 찍은 뒤에야 선이 보인다"는 지적이 있어 확인했는데, **첫 점을 찍고
마우스를 움직이면 커서까지 선이 따라옵니다.** 파동 도구도 마찬가지로 다음
라벨이 커서에 붙어 미리 보입니다. 스크린샷으로 확인했습니다. 다만 **손가락
(터치)에는 미리보기가 없습니다** — 화면을 누르기 전에는 커서라는 게 없기
때문이고, 트레이딩뷰 휴대폰 앱도 같습니다.

### 볼륨·RSI 도 원래 됩니다

`거래량`·`RSI` 단추가 이미 있고 눌러 봤습니다. 캔들 아래에 판이 따로 생깁니다.

### 화면 전체를 고딕으로

제목에 쓰던 명조(Noto Serif KR)를 걷어내고 **전부 고딕**(IBM Plex Sans KR)으로
바꿨습니다. 글꼴로 주던 대비는 굵기(600)와 자간으로 옮겼습니다.

### AI 테마의 질문 한 줄

"엔비디아 실적이 잘 나왔는데" 로 시작해서, 엔비디아가 빠지는 날에는 틀린 말처럼
읽혔습니다. **오류는 아니고 손으로 써 둔 예시 질문**이지만, 어느 날에 읽어도
맞도록 **"엔비디아가 크게 간 날, 그 돈은 어느 층으로 흘러갔나"** 로 바꿨습니다.
이 사이트가 실제로 세는 것(동조율)과 같은 물음이기도 합니다.

---

## 2026-08-26 · 마우스 그리기 검증과 모바일 손보기

### 마우스로 실제 그려지는지 — 통과

그동안 좌표를 코드로 넣어서만 확인했었습니다. iframe 안에 진짜 마우스
이벤트를 쏘는 방식으로 다시 확인했고, **여덟 도구가 전부 그려집니다**
(피보나치·추세선·수평선·12345 파동·ABC 조정·abcde 삼각형·채널·평행선).

첫 시도는 실패했는데 원인이 검증 쪽이었습니다. klinecharts 는 좌표를
`clientX` 가 아니라 **`pageX` 로 읽고**, 클릭 간격이 짧으면 더블클릭으로
뭉칩니다. 나중에 같은 검증을 다시 만들 일이 있으면 이 둘을 기억하세요.

### 피보나치 라벨이 겹치던 것

좁은 구간을 잡으면 눈금 일곱 개가 한 뼘에 몰려 라벨이 포개졌습니다.
**선은 전부 긋되 라벨만 솎아냅니다** — 양 끝은 항상 남기고, 중간은
61.8 → 38.2 → 50 → 23.6 → 78.6 순으로 자리가 날 때만 붙입니다.
좁게 잡으면 3개, 넉넉히 잡으면 7개가 나오는 것을 눈으로 확인했습니다.

### 휴대폰에서 차트 훈련이 못 쓸 물건이었습니다

폭 390px 에서 **봉이 스무 개쯤만 보였습니다.** 앞날을 그릴 빈 자리 260px
를 그대로 비워 두니 가격축 70px 을 빼고 봉이 설 자리가 60px 밖에 안 남은
탓입니다. 고친 것:

- 빈 자리를 **폭의 3분의 1**로 묶었습니다
- 새 문제를 열 때 화면 폭에 맞게 맞춥니다 — 좁으면 최근 70봉, 넓으면 180봉
- 봉 간격을 바꾸면 빈 자리가 초기화되므로 **맞춘 뒤 다시** 줍니다
  (이것 때문에 한 번 빈 자리가 통째로 사라졌습니다)

데스크톱은 그대로 180봉 + 빈 자리입니다.

### 손가락 크기 (390px 기준으로 전 화면 측정)

가로 스크롤은 원래 없었습니다. 문제는 터치 목표였습니다. 상단 메뉴·이름표·
꼬리말 링크·구획 선택기·단추·계좌 입력칸을 44px 로 올렸고, 카드 티커와 표
안 티커는 **보이는 크기는 두고 눌리는 범위만** 넓혔습니다(카드 42장이 전부
길어지는 것을 피하려고).

일부러 남긴 둘:
- **문장 안에 섞인 링크** — 키우면 줄바꿈이 깨져 글이 읽기 나빠집니다
- **태그 칩(28px)** — 촘촘히 붙어 있어 키우면 옆 칩 자리를 뺏습니다.
  접근성 최소치(24px)는 넘습니다

상단 메뉴가 390px 에서 18px 쯤 넘쳐 "읽는 법" 이 잘려 보이던 것도
간격·여백을 줄여 한 줄에 넣었습니다.

### 브리핑을 날짜별로 쌓기 시작했습니다

`briefing.json` 은 **오늘 것만** 담고 내일이면 덮어씁니다. 그래서 "이번 주에
어느 층이 계속 앞에 있었나" 를 알 방법이 없었습니다. 뉴스 아카이브와 같은
이유 — 지나가면 되찾을 수 없으니 **오늘부터** 쌓습니다.

- 갱신할 때마다 `briefing-history.json` 에 그날 기록을 덧붙입니다 (90일치 유지)
- 하루치 전부가 아니라 뒤에서 다시 볼 것만 남깁니다 — 가장 뜨거운 층,
  올라선 층, 내려앉은 층, 자리바꿈 여부. 하루 2.5KB 라 90일이면 225KB
- 같은 날 두 번 돌리면 덮어씁니다
- 홈에 **"지난 N일 동안"** 구획이 붙습니다. 테마마다 *가장 여러 날 1위였던 층*
  과 *자리바꿈이 있었던 날 수* 를 "며칠 중 며칠" 로 적습니다

**사흘도 안 쌓였으면 구획이 스스로 사라집니다.** 그래서 지금(1일치)은 홈에
안 보이고, 다음 주쯤 저절로 나타납니다. 반쯤 채워진 표를 라이브에 두지
않으려는 것이고, 분모를 밝히는 것은 동조율과 같은 이유입니다.

### 차트 훈련 기록이 남습니다

한 판이 끝나면 내가 뭘 그었는지 사라졌습니다. 이제 **"이 판 기록에 남기기"**
로 남길 수 있고, 머리의 **기록 N** 을 누르면 지난 판들이 열립니다.

맨 위에 **"내가 자주 긋는 것"** 을 도구별로 셉니다. 이게 이 화면의 이유입니다 —
한 판씩 볼 때는 안 보이지만 스무 판이 쌓이면 "나는 추세선만 긋고 되돌림은
거의 안 본다" 같은 것이 드러납니다.

남기는 것: 종목·가린 날·열어본 봉 수·그 구간의 변화율과 고저·도구별 개수·
내가 그은 수평선 중 실제가 닿은 수·직접 적는 한 줄.

- **점수를 매기지 않습니다.** 잘 그었다·못 그었다를 우리가 판단하면 그건
  조언이 되고, 지켜야 할 선을 넘습니다. 사실만 적습니다
- **이 기기에만** 저장됩니다(localStorage). 매매노트·워치리스트와 같은 방식
- 같은 판을 다시 저장하면 **덮어씁니다.** 봉을 더 열어 보고 다시 남길 때
  두 줄이 생기면 도구 집계가 부풀려지기 때문입니다

전 과정을 실제 마우스로 돌려 확인했습니다 — 네 개 그리기 → 실제 보기 →
저장 → 기록 열기 → 봉 더 열고 재저장(1건 유지, 숫자만 갱신). 데스크톱과
390px 양쪽 화면도 확인했습니다.

**알아 둘 것**: 점 두 개를 **0.5초 안에** 연달아 찍으면 두 번째가 더블클릭으로
뭉쳐 사라집니다. klinecharts 자체 동작이고 트레이딩뷰도 비슷합니다. 검증
중에 이것 때문에 추세선 하나가 사라져 한참 원인을 찾았습니다.

### 아직 확인 못 한 것

"지난 N일 동안" 구획은 가짜 5일치를 넣어 데스크톱·모바일 모양만 확인했습니다.
**실제 숫자로는 사흘이 지나야 보입니다.**

**측정 방법**: 폭을 강제한 iframe 으로 감싼 페이지를 만들어 헤드리스
브라우저로 재고, 누를 수 있는 범위는 `elementFromPoint` 로 확인합니다.
헤드리스 창은 390px 아래로 못 줄어들기 때문에 iframe 이 필요합니다.
검증 파일은 `public/` 에 두고 쓴 뒤 **반드시 지웁니다** — 공개 저장소입니다.

---

## 2026-08-25 (오후) · 차트 훈련과 그리기 도구

### 훈련 방식을 바꿨습니다

처음엔 "20거래일 뒤 오를까 내릴까" 맞히기로 만들었는데, 그건 동전 던지기지
차트 읽는 실력과 상관이 없다는 지적을 받고 **겹쳐 보기**로 다시 만들었습니다.

```
과거 어느 날까지만 캔들 → 오른쪽 빈 자리에 앞날을 그림
→ "실제 보기" → 그 자리에 진짜 캔들이 채워짐
→ 내가 그은 선 위로 실제가 지나가는 걸 눈으로 봄
```

이게 되려면 **미래 시각에 선을 그을 수 있고, 나중에 진짜 봉이 그 자리를
채울 때 선이 제자리에 남아야** 합니다. 검증판으로 확인했습니다 — 미래로 뻗는
선·미래에만 있는 선·목표가 수평선 셋 다 그려졌고, 봉 60개를 채운 뒤에도
좌표가 그대로였습니다.

### 10회 스트레스 검증

10개 종목에서 매회 도형 31개(12345 임펄스 + ABC + abcde + 채널 3 + 피보나치 +
지지저항 2 + 예상경로 2)를 그었습니다.

**총 310개, 실패 0건, 실제 캔들 40개를 채운 뒤 좌표 보존 100%.**
IONQ·MARA 같은 극단적 변동성 종목에서도 같았습니다.

### 피보나치·파동 도구는 직접 만들었습니다

klinecharts 기본 피보나치는 선이 화면 끝까지 뻗고, 비율 라벨이 왼쪽에 파란
알약으로 뭉치고, 구간에 색이 없었습니다. `registerOverlay` 로 새로 만들었습니다
(`src/components/practice/overlays.ts`):

- `fibRetracement` — 찍은 두 점 사이에만, 구간마다 옅은 색, 배경 없는 라벨
- `elliottImpulse` / `elliottCorrection` / `elliottTriangle` — 점만 찍으면
  1~5 / A B C / a b c d e 가 자동으로 붙음

### 아직 확인 못 한 것

**마우스로 실제 클릭해서 그리는 것은 검증하지 못했습니다.** 좌표를 코드로
넣어 그린 것만 확인했습니다. klinecharts 가 `totalStep` 으로 단계별 클릭을
처리하니 될 것으로 보지만, 직접 눌러보셔야 확실합니다.

`/practice` 에서 파동 도구를 눌러 점을 찍어보시고 어색한 부분을 말씀해 주세요.

---

## 2026-08-25 · 2단계 (가격 데이터 붙이기) 완료

### 지금 돌아가는 것

```bash
cd "c:\dev\claude code\theme-map"
npm run dev        # http://localhost:3000
npm run update     # 시세 다시 받아 계산 (1~2분)
npm run validate   # 종목이 아직 다 살아있는지만 확인
```

깃허브: **https://github.com/banghozin/k7q2vx** (공개)

### 사장님이 하셔야 할 일 — 딱 하나

**Vercel 연결이 안 됐습니다.** 제가 붙은 Vercel 계정(`seo-128`)이 깃허브
`banghozin` 저장소에 접근 권한이 없습니다. stocksnet을 돌리는 계정과 다른
계정으로 연결돼 있는 것 같습니다. 브라우저에서 권한을 주는 일이라 제가
대신 못 합니다.

> **stocksnet을 돌리는 그 Vercel 계정**으로 로그인
> → [vercel.com/new](https://vercel.com/new)
> → 목록에서 **`k7q2vx`** 선택 → Deploy
>
> 목록에 안 보이면 "Adjust GitHub App Permissions" → `k7q2vx` 체크

한 번만 하시면 그 뒤로는 제가 push할 때마다 자동 배포됩니다.
**배포 횟수는 데이터 갱신 기준 하루 1회**입니다 (한 달 약 22회). 기존 사이트
한도에 영향 없습니다.

그리고 깃허브 저장소의 **Actions 탭**에서 "시세 갱신"을 한 번 수동 실행
(`Run workflow`) 해 보시면 자동 갱신이 실제로 도는지 확인됩니다. 아직 제가
실행해 보지 못했습니다 — push 직후라 워크플로가 등록되는 시간이 필요합니다.

### 폰으로 직접 봐 주셔야 할 것

반응형은 코드로만 검증했습니다. **실제 화면은 제가 못 봅니다.** 폰에서
확인해 주실 것:

- 테마 페이지의 **동조율 표**가 가로 스크롤 없이 카드로 보이는지
- 별표(★) 버튼이 손가락으로 눌리는 크기인지 (28px → 44px로 키웠습니다)
- 층 온도 지도 막대가 잘리지 않는지
- 상단 바가 스크롤할 때 따라오는지 (`overflow-x: hidden`을 넣었다 뺐습니다.
  넣으면 sticky가 죽어서요)

---

### 한 일

**데이터 파이프라인**
- `npm run update` 로 190종목 2년 일봉 수집 → 층별 온도·동조율·대장주 계산
- 21초, 93,258봉, 실패 0건
- 계산 결과 4개 파일 91KB. 원본 시세(2.6MB)는 `.cache/`에 두고 커밋 안 함

**화면**
- 층 머리에 5일/20일 중앙값·상승 종목 수·테마 내 순위
- 종목 카드에 종가·1일·5일·20일·지수 대비, 대장 배지
- 층 온도 지도, 동조율 표, 대장주 근거 분해표
- 홈: 가장 뜨거운/식은 층, 손바뀜 발생 테마
- 읽는 법 페이지에 계산 방식 전문

**자동 갱신**
- `.github/workflows/update-prices.yml` — 평일 미국장 마감 후 UTC 22:17
- 갱신 앞단에 티커 생존 검사. 실패하면 멈추고 이슈로 알림

---

### 도중에 발견한 문제와 대응

**① stooq를 못 씁니다 (CLAUDE.md 4번 전제가 깨짐)**
`https://stooq.com/q/d/l/?s=nvda.us&i=d` 가 CSV 대신 자바스크립트 봇 검증
페이지를 돌려줍니다. Yahoo Finance로 전환했습니다. CLAUDE.md에도 적어뒀습니다.

**② 큐레이션한 종목 5개가 이미 죽어 있었습니다**
188개 전수 조회로 잡았습니다.

| 뺀 것 | 이유 | 넣은 것 |
|---|---|---|
| CYBR | 팔로알토에 피인수·상장폐지 | SAIL (세일포인트) |
| IRBT | 상장폐지 | PRCT (프로셉트 바이오로보틱스) |
| LICY | 상장폐지 | NVX (노보닉스) |
| SMLR | 스트라이브에 합병 | ASST (스트라이브) |
| BK | 티커 변경 | BNY |

→ 다시는 조용히 생기지 않도록 **갱신 앞단에 생존 검사**를 넣었습니다.

**③ 대장주 지표를 세 번 갈아엎었습니다**

이게 오늘 가장 오래 걸린 일입니다. 처음 나온 결과가 상식과 어긋나서요.

1. **1차** 거래대금 절대규모를 썼더니 → 양자컴퓨팅 대장이 **MSFT**,
   사이버보안 대장도 **MSFT**. 대형주가 곁다리로 낀 테마에서도 무조건 이깁니다.
   게다가 11개 테마 전부에서 "손바뀜"이 나왔습니다. 전부 바뀌면 그건 잡음입니다.
2. **2차** 시장(S&P500) 영향을 걷어냈더니 대형주 편향은 사라졌는데,
   자율주행 대장에 **에어백 회사(ALV)**, 로봇 대장에 **모터 회사(AME)**.
   시장이 오르는 날에만 오르는 저변동 종목이 "같이 올랐다"로 집계된 탓입니다.
3. **3차** 종목마다 표본을 상위 30일로 통일하고, "이 회사에게 이 테마는
   곁다리"인 종목을 대장 후보에서 뺐습니다 → 양자 대장이 **IONQ**로 정착.

지금 결과 (2026-08-24 기준):

| 테마 | 대장 | 비고 |
|---|---|---|
| AI 반도체 | ASML | 접전 · 손바뀜 |
| 전력·원전 | NRG | 접전 |
| 로봇 | ROK | 접전 · 손바뀜 |
| 양자컴퓨팅 | IONQ | |
| 우주·방산 | LMT | 접전 · 손바뀜 |
| 비만치료제 | AMGN | |
| 사이버보안 | RPD | |
| 자율주행 | APTV | 손바뀜 |
| 전기차·배터리 | MP | 손바뀜 |
| 조선·해운 | STNG | 접전 · 손바뀜 |
| 디지털자산 | MARA | 접전 · 손바뀜 |

1·2위가 붙어 있으면 **"접전"으로 표시하고 한 종목을 단정하지 않습니다.**

**④ 계산 파일이 없으면 빌드가 통째로 실패했습니다**
새로 클론한 사람이 `npm run build`만 치면 깨집니다. `prebuild`로 빈 파일을
만들어 두게 했습니다. 지금은 데이터 없이도 층 구조가 그대로 뜹니다.

**⑤ 시세 JSON 80KB가 브라우저 번들에 실려가고 있었습니다**
표기 함수(`pct`, `tone`)를 `lib/format.ts`로 떼어내 해결했습니다.

---

### 규제 검토 결과 (2026-08-25 확인)

유사투자자문업은 **"고객으로부터 일정한 대가를 받고"** 투자판단에 조언하는
것이 요건입니다. 우리는 무료이고 조언이 아니라 사실 서술이라 **두 요건 모두
해당하지 않습니다.** 신고 대상이 아닙니다.

다만 지켜야 할 선 두 가지를 못 박아 둡니다:

1. **유료화하면 다시 검토해야 합니다.** 구독료·유료 회원 등 이용자에게서
   돈을 받는 순간 첫 번째 요건이 성립합니다. (광고 수익은 이용자가 아니라
   광고주에게서 받는 것이라 성격이 다르지만, 그때도 확인이 필요합니다.)
2. **매수·매도 판단 표현 금지는 계속 유지합니다.** 신고 의무와 별개로,
   근거 없는 단정은 손해배상 위험이 있고 이 사이트의 정체성과도 어긋납니다.

화면에 이미 반영한 것:
- "대장주"는 계산된 관찰이지 추천이 아니라는 문구를 배지와 표에 붙였습니다
- 동조율 표 머리에 "지나간 기록이며 앞으로도 같이 움직인다는 뜻이 아니다"를
  작게 숨기지 않고 표 바로 위에 뒀습니다

---

### 추가로 만든 것 (계획에 없었지만 시간이 남아서)

**① 매일 한 줄 브리핑** — CLAUDE.md 7번의 5단계입니다.

같은 층들을 20일 성과로 한 번, 5일 성과로 한 번 줄 세워 **순위가 몇 계단
움직였는지**를 봅니다. 수익률 차이가 아니라 순위 차이를 쓰는 이유는, 시장
전체가 빠진 주에는 모든 층의 수익률이 같이 내려가 비교가 안 되기 때문입니다.

지금 나오는 문장 (2026-08-24 기준):

> **AI 반도체** — 최근 5일 기준으로 1층 GPU·가속기 설계가 5계단 올라섰고,
> 5층 광통신·네트워크가 4계단 내려앉았습니다. 20일 기준 가장 앞선 층은
> 8층 클라우드·모델·소프트웨어 +18.8%입니다.

> **로봇·휴머노이드** — 5층 산업 자동화가 4계단 올라섰고, 2층 센서·비전이
> 4계단 내려앉았습니다.

문장에 원인을 넣지 않았습니다. &ldquo;돈이 옮겨갔다&rdquo;는 해석이고, 우리가 아는 건
&ldquo;순위가 바뀌었다&rdquo;는 사실뿐입니다.

한글 조사도 받침에 맞춰 고르게 했습니다(`설계가` / `센서·비전이`). 층 이름이
데이터에서 오기 때문에 이걸 안 하면 &ldquo;설계이&rdquo;, &ldquo;네트워크을&rdquo; 같은 게 섞여
바로 기계가 쓴 티가 납니다.

**② 동조율 기준 종목 바꾸기**

계산으로 뽑은 대장이 기본이지만 &ldquo;NVDA 기준으로 보면?&rdquo;은 당연히 나오는
질문이라, 상위 후보 3개 + 그 테마 최대 거래대금 종목을 미리 계산해 뒀습니다.
이렇게 해서 **NVDA**(로봇·양자·자율주행), **TSLA**(전기차), **LLY**(비만),
**MSTR·COIN**(디지털자산), **PLTR**(우주)가 후보에 들어왔습니다.

---

### ③ 층 순환을 시간축으로 (2026-08-25 추가)

이 사이트가 하겠다고 한 것은 "돈이 어느 층에서 어느 층으로 옮겨갔는가"인데,
그동안 화면은 **오늘 한 시점**만 보여줬습니다. 이제 지난 반년을 5거래일
간격으로 되짚어 층 순위 변화를 선으로 보여줍니다.

AI 테마에서 실제로 나온 그림:

| 층 | 반년 전 | 지금 |
|---|---|---|
| 8F 클라우드·모델·소프트웨어 | **8위 (꼴찌)** | **1위** |
| 4F HBM·메모리 | **1위** | 5위 |

메모리는 반년 내내 1위를 지키다 최근 무너졌고, 클라우드가 꼴찌에서 1위로
올라섰습니다. CLAUDE.md 8번에 적어 둔 *"메모리는 상대적 약세"*가 데이터로
확인된 셈입니다.

**수익률이 아니라 순위를 그리는 이유**: 시장 전체가 빠진 구간에는 모든 층의
수익률이 같이 내려가 선이 뭉쳐 비교가 안 됩니다. 순위는 그 공통 요인에
영향을 받지 않습니다.

**색을 여덟 갈래로 쓰지 않은 이유**: 층이 8개라고 색을 8개 쓰면 무지개가 되고
색각 이상이 있는 분은 구분하지 못합니다. **색은 한 가지 일만** 합니다 —
가장 많이 올라선 층(빨강)과 내려앉은 층(파랑) 둘만 칠하고 나머지는 회색.
어느 선이 어느 층인지는 **선 끝의 이름표**로 알 수 있어 색만으로 정보를
전달하지 않습니다. 세 색은 배경 대비·색각 구분·정상시야 구분 검사를
통과한 조합입니다. 표로도 볼 수 있게 해 뒀습니다.

만들면서 렌더링해 보고 **가장 긴 층 이름이 잘리는 것**을 발견해 오른쪽
여백을 168px → 210px 로 넓혔습니다.

---

### 남은 리서치 결과 (2026-08-25)

**경쟁 서비스** — CLAUDE.md 5번 표가 여전히 맞습니다. 알파스퀘어 테마는
지금도 국내 실시간 테마주 순위 중심이고, 초이스스탁US 는 밸류에이션·타이밍
도구입니다. **미국 종목을 밸류체인 층으로 세운 한국어 서비스는 없습니다.**

**투자자 쪽** — 커뮤니티 불만 글은 검색으로 직접 확인하지 못했습니다. 대신
인용하기 더 좋은 근거를 찾았습니다:

> 한국은행 — 개인의 해외주식 투자가 **"M7 등 인지도가 높은 종목과 레버리지
> ETF 에 쏠렸다"**

**아는 이름에 몰리는 건 나머지 체인을 찾을 방법이 없어서**입니다. 층 지도가
푸는 문제가 정확히 이것이라, 나중에 사이트 소개 글을 쓸 때 쓸 만한 근거입니다.

---

### 아직 안 한 것

- ~~차트 훈련에서 마우스로 직접 그리기 확인~~ → 2026-08-26 통과
- ~~브리핑을 날짜별로 쌓아 두기~~ → 2026-08-26 시작
- ~~그린 분석과 결과를 저장해 두기~~ → 2026-08-26 완료
- 폰 실물 확인 (위 "폰으로 직접 봐 주셔야 할 것" 참고)
- stocksnet 쪽 cron-job.org 정리 — 저장소 밖이라 제가 못 건드립니다.
  "Update market data" 를 찌르는 외부 job 은 GitHub cron 과 중복입니다
  (다만 50분 중복 방지 장치를 넣어 무해하게 만들어 뒀습니다)

### 지금 열려 있는 것 (2026-08-26 심야 기준)

**확인이 필요한 것**

- **뉴스 6시간 수집의 첫 예약분** — 새로 만든 워크플로라 아직 안 돌았습니다
- **"지난 N일 동안"** 구획 — 브리핑이 사흘 쌓여야 실제 숫자로 나옵니다
  (지금은 스스로 숨어 있음)
- **폰 실물** — 헤드리스로만 쟀습니다. 특히 손가락으로 그리기

**손대지 않기로 한 것**

- 한국 공급망 기사(삼성 HBM 등)를 뉴스에 넣는 것 — 이 매체가 산업 취재를
  거의 안 해서 실제로 걸릴 게 없습니다. 공급원을 하나 더 붙이면 그때
- 테마 기사가 0건인 날 채우기 — 멕시코군 기사로 채우느니 비워 두는 쪽

**아직 안 한 것**

- 분석 화면에 그린 것을 **매매노트와 잇기** — "이 선을 보고 샀다" 가 이어지면
  훈련 → 분석 → 매매가 한 줄이 됩니다
- 분석 화면 그림을 **기기 밖으로 백업**하기 (지금은 브라우저를 지우면 사라짐).
  매매노트처럼 JSON 내보내기를 붙이면 됩니다

---

## 2026-08-25 (오후) · SBH 뉴스를 여섯 곳에 붙임

뉴스가 테마 페이지 맨 아래 한 구획에서만 쓰이고 있었고, **읽어서 보여주기만
하고 저장을 안 하고 있었습니다.** 피드는 최근 100건만 주는데 재어 보니 그게
7시간 반이면 다 찹니다 — **하루만 안 모으면 그날 기사는 영영 사라집니다.**

붙인 자리: ① 아카이브(파이프라인) ② 차트 모달의 종목별 기사 ③ 층 머리의
근거 기사 ④ 매매노트의 진입일 기사 ⑤ 홈 헤드라인 ⑥ 차트에 기사 나온 날 표시.

**한국어 이름 매칭이 까다로웠습니다.** 띄어쓰기 없이 조사가 붙어서
"인텔리시아"가 인텔로, "삼성전자 갤럭시 버즈"가 갤럭시 디지털로 걸렸습니다.
앞에 한글이 붙으면 빼고 뒤는 조사일 때만 같은 낱말로 보게 고쳤고,
겪은 오탐을 시험 항목으로 남겼습니다 (`npm run test:news`, 14건 통과).

**하루에 잡히는 기사는 1~3건**입니다. 국제정치 매체라 미국 개별종목 기사
비중이 낮습니다. 피드가 하루 3번쯤 도니 5~10건씩 쌓입니다.
**정확도를 택하고 양을 포기한 판단**입니다 — 엉뚱한 기사가 종목에 붙는 게
기사 몇 건 놓치는 것보다 나쁩니다.

---
