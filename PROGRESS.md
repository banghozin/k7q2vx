# 진행 기록

> 사장님이 자리를 비운 동안 한 일과, 돌아와서 봐 주셔야 할 것.
> 최근 것이 위입니다.

---

## 2026-08-25 · 2단계 (가격 데이터 붙이기) 완료

### 지금 돌아가는 것

```bash
cd "c:\dev\claude code\theme-map"
npm run dev        # http://localhost:3000
npm run update     # 시세 다시 받아 계산 (1~2분)
npm run validate   # 종목이 아직 다 살아있는지만 확인
```

깃허브: **https://github.com/banghozin/k7q2vx** (공개)

### 사장님이 하셔야 할 일 — 딱 하나

**Vercel 연결이 안 됐습니다.** 제가 붙은 Vercel 계정(`seo-128`)이 깃허브
`banghozin` 저장소에 접근 권한이 없습니다. stocksnet을 돌리는 계정과 다른
계정으로 연결돼 있는 것 같습니다. 브라우저에서 권한을 주는 일이라 제가
대신 못 합니다.

> **stocksnet을 돌리는 그 Vercel 계정**으로 로그인
> → [vercel.com/new](https://vercel.com/new)
> → 목록에서 **`k7q2vx`** 선택 → Deploy
>
> 목록에 안 보이면 "Adjust GitHub App Permissions" → `k7q2vx` 체크

한 번만 하시면 그 뒤로는 제가 push할 때마다 자동 배포됩니다.
**배포 횟수는 데이터 갱신 기준 하루 1회**입니다 (한 달 약 22회). 기존 사이트
한도에 영향 없습니다.

그리고 깃허브 저장소의 **Actions 탭**에서 "시세 갱신"을 한 번 수동 실행
(`Run workflow`) 해 보시면 자동 갱신이 실제로 도는지 확인됩니다. 아직 제가
실행해 보지 못했습니다 — push 직후라 워크플로가 등록되는 시간이 필요합니다.

### 폰으로 직접 봐 주셔야 할 것

반응형은 코드로만 검증했습니다. **실제 화면은 제가 못 봅니다.** 폰에서
확인해 주실 것:

- 테마 페이지의 **동조율 표**가 가로 스크롤 없이 카드로 보이는지
- 별표(★) 버튼이 손가락으로 눌리는 크기인지 (28px → 44px로 키웠습니다)
- 층 온도 지도 막대가 잘리지 않는지
- 상단 바가 스크롤할 때 따라오는지 (`overflow-x: hidden`을 넣었다 뺐습니다.
  넣으면 sticky가 죽어서요)

---

### 한 일

**데이터 파이프라인**
- `npm run update` 로 190종목 2년 일봉 수집 → 층별 온도·동조율·대장주 계산
- 21초, 93,258봉, 실패 0건
- 계산 결과 4개 파일 91KB. 원본 시세(2.6MB)는 `.cache/`에 두고 커밋 안 함

**화면**
- 층 머리에 5일/20일 중앙값·상승 종목 수·테마 내 순위
- 종목 카드에 종가·1일·5일·20일·지수 대비, 대장 배지
- 층 온도 지도, 동조율 표, 대장주 근거 분해표
- 홈: 가장 뜨거운/식은 층, 손바뀜 발생 테마
- 읽는 법 페이지에 계산 방식 전문

**자동 갱신**
- `.github/workflows/update-prices.yml` — 평일 미국장 마감 후 UTC 22:17
- 갱신 앞단에 티커 생존 검사. 실패하면 멈추고 이슈로 알림

---

### 도중에 발견한 문제와 대응

**① stooq를 못 씁니다 (CLAUDE.md 4번 전제가 깨짐)**
`https://stooq.com/q/d/l/?s=nvda.us&i=d` 가 CSV 대신 자바스크립트 봇 검증
페이지를 돌려줍니다. Yahoo Finance로 전환했습니다. CLAUDE.md에도 적어뒀습니다.

**② 큐레이션한 종목 5개가 이미 죽어 있었습니다**
188개 전수 조회로 잡았습니다.

| 뺀 것 | 이유 | 넣은 것 |
|---|---|---|
| CYBR | 팔로알토에 피인수·상장폐지 | SAIL (세일포인트) |
| IRBT | 상장폐지 | PRCT (프로셉트 바이오로보틱스) |
| LICY | 상장폐지 | NVX (노보닉스) |
| SMLR | 스트라이브에 합병 | ASST (스트라이브) |
| BK | 티커 변경 | BNY |

→ 다시는 조용히 생기지 않도록 **갱신 앞단에 생존 검사**를 넣었습니다.

**③ 대장주 지표를 세 번 갈아엎었습니다**

이게 오늘 가장 오래 걸린 일입니다. 처음 나온 결과가 상식과 어긋나서요.

1. **1차** 거래대금 절대규모를 썼더니 → 양자컴퓨팅 대장이 **MSFT**,
   사이버보안 대장도 **MSFT**. 대형주가 곁다리로 낀 테마에서도 무조건 이깁니다.
   게다가 11개 테마 전부에서 "손바뀜"이 나왔습니다. 전부 바뀌면 그건 잡음입니다.
2. **2차** 시장(S&P500) 영향을 걷어냈더니 대형주 편향은 사라졌는데,
   자율주행 대장에 **에어백 회사(ALV)**, 로봇 대장에 **모터 회사(AME)**.
   시장이 오르는 날에만 오르는 저변동 종목이 "같이 올랐다"로 집계된 탓입니다.
3. **3차** 종목마다 표본을 상위 30일로 통일하고, "이 회사에게 이 테마는
   곁다리"인 종목을 대장 후보에서 뺐습니다 → 양자 대장이 **IONQ**로 정착.

지금 결과 (2026-08-24 기준):

| 테마 | 대장 | 비고 |
|---|---|---|
| AI 반도체 | ASML | 접전 · 손바뀜 |
| 전력·원전 | NRG | 접전 |
| 로봇 | ROK | 접전 · 손바뀜 |
| 양자컴퓨팅 | IONQ | |
| 우주·방산 | LMT | 접전 · 손바뀜 |
| 비만치료제 | AMGN | |
| 사이버보안 | RPD | |
| 자율주행 | APTV | 손바뀜 |
| 전기차·배터리 | MP | 손바뀜 |
| 조선·해운 | STNG | 접전 · 손바뀜 |
| 디지털자산 | MARA | 접전 · 손바뀜 |

1·2위가 붙어 있으면 **"접전"으로 표시하고 한 종목을 단정하지 않습니다.**

**④ 계산 파일이 없으면 빌드가 통째로 실패했습니다**
새로 클론한 사람이 `npm run build`만 치면 깨집니다. `prebuild`로 빈 파일을
만들어 두게 했습니다. 지금은 데이터 없이도 층 구조가 그대로 뜹니다.

**⑤ 시세 JSON 80KB가 브라우저 번들에 실려가고 있었습니다**
표기 함수(`pct`, `tone`)를 `lib/format.ts`로 떼어내 해결했습니다.

---

### 규제 검토 결과 (2026-08-25 확인)

유사투자자문업은 **"고객으로부터 일정한 대가를 받고"** 투자판단에 조언하는
것이 요건입니다. 우리는 무료이고 조언이 아니라 사실 서술이라 **두 요건 모두
해당하지 않습니다.** 신고 대상이 아닙니다.

다만 지켜야 할 선 두 가지를 못 박아 둡니다:

1. **유료화하면 다시 검토해야 합니다.** 구독료·유료 회원 등 이용자에게서
   돈을 받는 순간 첫 번째 요건이 성립합니다. (광고 수익은 이용자가 아니라
   광고주에게서 받는 것이라 성격이 다르지만, 그때도 확인이 필요합니다.)
2. **매수·매도 판단 표현 금지는 계속 유지합니다.** 신고 의무와 별개로,
   근거 없는 단정은 손해배상 위험이 있고 이 사이트의 정체성과도 어긋납니다.

화면에 이미 반영한 것:
- "대장주"는 계산된 관찰이지 추천이 아니라는 문구를 배지와 표에 붙였습니다
- 동조율 표 머리에 "지나간 기록이며 앞으로도 같이 움직인다는 뜻이 아니다"를
  작게 숨기지 않고 표 바로 위에 뒀습니다

---

### 아직 안 한 것

- **매일 한 줄 브리핑** (CLAUDE.md 7번 5단계) — "오늘 AI 체인에서 가장
  뜨거웠던 층은 광통신" 같은 자동 요약. 재방문 장치이자 유튜브 소재
- **층 순환을 시간축으로** 보여주는 화면 — 지금은 오늘 한 시점만 보입니다
- 동조율의 기준 종목을 화면에서 바꿔 보는 기능 (지금은 자동 판별값 고정)
